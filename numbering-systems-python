# In the "Numbering Systems with Python" tutorial we will utilize Python to begin to understand the 
# practical side of:
# 1 - Algorithmic thinking
# 2 - Representation
# 3 - Programming
# 4 - Design
#
# We will do this by developing a Python application that can convert strings to numbers (base 10) 
# and base 10 numbers to hexadecimal numbers.

# Enhancement #1: Replace above header information with appropriate header information including 
# full name, email address, and credits. 

import sys 

print("\nNumbering Systems with Python")
numberOfArgs = len(sys.argv)
print("Total arguments passed: " + str(numberOfArgs))
print("Argument 1: " + sys.argv[0])

# Enhancement #2: Check for an appropriate number of arguments and provide meaningful error messages if input
# is not appropriate. 

if numberOfArgs == 2:
	print("Argument 2: " + sys.argv[1])
	numberAsAString = sys.argv[1]
	numberAsAnInt = int(numberAsAString, base=10)
	numberAsHex = hex(numberAsAnInt)

	print("Input:  " + numberAsAString)
	print("Number: " + str(numberAsAnInt))
	print("Hex:    " + numberAsHex)

# Enhancement #3: Update the application so that it can accept up to 5 (base 10) numbers as arguments and
# display the Input, Number, and Hex of hex value of each of the numbers. 

# Enhancement #4: Add appropriate error checking and user friendly error messages for input parameters. 

print("")
